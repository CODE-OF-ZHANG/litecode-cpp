# =============================================================
# LiteCode-CPP — judge/ .dockerignore
# =============================================================
# 判题镜像构建上下文只关心 Dockerfile + judge.sh + lib/*.sh。
# 避免把测试脚本、构建产物、git 历史等拷进上下文。
# =============================================================

*
!Dockerfile
!judge.sh
!lib/
!.dockerignore
