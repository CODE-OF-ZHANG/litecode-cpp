# =============================================================
# LiteCode-CPP — judge/ .dockerignore
# =============================================================
# 判题镜像构建上下文只关心 Dockerfile + judge.sh。
# 避免把 C++ 源码、构建产物等大文件拷进上下文。
# =============================================================

# 仅保留 Dockerfile + judge.sh
*
!Dockerfile
!judge.sh
!.dockerignore